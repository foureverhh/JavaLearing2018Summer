package Youtube;

public class StringBufferTest {
    StringBuffer stringBuffer = new StringBuffer("Hello");
    // string buffer works in a independent thread, is not thread safe when compare with StringBuilder

}
